import UIKit

extension UIColor { 
    // 15 Xcode автоматически создал extensions
    // для необходимых цветов. Файл решил оставить, так как
    // не знаю - может он дальше понадобиться или нет.
    // На данный момент в нём нет необходимости.
}
